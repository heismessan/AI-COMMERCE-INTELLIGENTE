[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && bash postinstall.sh"

[deploy]
startCommand = "gunicorn api:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
